#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: string to check
 *
 * Return: length of a string.
 */
int _strlen(char *s)
{
	int i;

	length = 0;

	while (s[length] != '\0')
	{
		length++;
	}

	return (lenght);
}

