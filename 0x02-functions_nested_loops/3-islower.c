#include "main.h"
/**
 * _islower - check if a charcter is lower.
 *
 * @c: The character to be checked
 *
 * Return: 1 if c is lower, 0 otherwise.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
